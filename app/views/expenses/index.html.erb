


<div style="display: flex; justify-content: center;">
  <div class="card">
    <div class="card-header">
      <h1><h1>Novo Plano de Custo</h1>
    </h1>
    </div>
    <%= form_with(url: create_expense_path) do |form| %>
      <div>
          <%= form.hidden_field :account_plan_id, value:@account_plan.id %>
      </div>
      <div>
        <%= form.label :name %>
          <%= form.text_field :name %>
      </div>
      <div>
        <%= form.submit %>
      </div>
      <% end %>
        <%= link_to 'Back' , account_plans_path %>

  </div>